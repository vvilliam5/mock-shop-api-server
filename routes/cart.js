const express = require('express');
const router = express.Router;

//view all products in cart, by id of user
router.get('/:id', (res, req, next) => {

});
//add new product to cart using either product or user id
router.post('/:id', (res, req, next) => {

})
//delete a product from cart by cart id
router.delete('/:id', (res, req, next) => {

})